import {Layout, List} from '@ui-kitten/components';
import styled from 'styled-components';

export const ChatListBox = styled(Layout)``;

export const ListScrollView = styled(List)`
  height: 100%;
  margin-top: 15px;
`;
